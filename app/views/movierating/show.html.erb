<h1>View <%= pluralize(@movies.count, "Movie Ratings") %></h1>
<table class="table table-striped">
	<thead>
		<th>Title</th>
		<th>Rating</th>
	</thead>
	<tbody>
		<% @movies.each do |movie| %>
		<tr>
			<td><%= movie.title %></td>
			<td>Rating</td>
		</tr>
		<% end %>
	</tbody>
</table>